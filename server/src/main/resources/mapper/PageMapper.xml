<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.catalog.mapper.PageMapper">
    <insert id="insert">
        insert into page(nickname, position, description, img_url, content, create_time, update_time)
        values(#{nickname}, #{position}, #{description}, #{imgUrl}, #{content}, #{createTime}, #{updateTime})
    </insert>
</mapper>